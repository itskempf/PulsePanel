
<Page x:Class="PulsePanel.Views.SchedulerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <TextBlock Text="Scheduler" FontSize="22" FontWeight="Bold"/>

    <StackPanel Grid.Row="1" Orientation="Horizontal" >
      <StackPanel.Resources><Style TargetType="TextBox"><Setter Property="Width" Value="160"/></Style><Style TargetType="Button"><Setter Property="Margin" Value="6,0,0,0"/></Style></StackPanel.Resources>
      <TextBox Text="{Binding Name}"/>
      <TextBox Text="{Binding Cron}"/>
      <TextBox Width="260" Text="{Binding Target}"/>
      <Button Content="Add" Command="{Binding Add}"/>
      <Button Content="Refresh" Command="{Binding Refresh}"/>
    </StackPanel>

    <ListView Grid.Row="2" ItemsSource="{Binding Tasks}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" DisplayMemberBinding="{Binding TaskName}" Width="200"/>
          <GridViewColumn Header="Cron" DisplayMemberBinding="{Binding CronSchedule}" Width="150"/>
          <GridViewColumn Header="Enabled" DisplayMemberBinding="{Binding IsEnabled}" Width="80"/>
          <GridViewColumn Header="Action" Width="120">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <Button Content="Delete" Command="{Binding DataContext.Remove, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</Page>
