
<Window x:Class="PulsePanel.Views.CreateServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Server" Height="480" Width="600" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" >
      <StackPanel.Resources><Style TargetType="TextBlock"><Setter Property="Margin" Value="0,0,8,0"/></Style></StackPanel.Resources>
      <TextBlock Text="Server Name:" VerticalAlignment="Center"/>
      <TextBox Width="300" Text="{Binding InstanceName, UpdateSourceTrigger=PropertyChanged}"/>
    </StackPanel>

    <Border Grid.Row="1" Background="{StaticResource ControlBackgroundBrush}" CornerRadius="6" Padding="10">
      <DockPanel>
        <TextBlock Text="Template Variables" DockPanel.Dock="Top" FontWeight="Bold"/>
        <ScrollViewer>
          <ItemsControl ItemsSource="{Binding Variables}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal" >
                  <StackPanel.Resources><Style TargetType="TextBlock"><Setter Property="Margin" Value="0,0,6,0"/></Style></StackPanel.Resources>
                  <TextBlock Text="{Binding Name}" Width="180"/>
                  <TextBox Text="{Binding Value}" Width="300"/>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </DockPanel>
    </Border>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" >
      <StackPanel.Resources><Style TargetType="Button"><Setter Property="Margin" Value="6,0,0,0"/></Style></StackPanel.Resources>
      <TextBlock Text="{Binding Status}" VerticalAlignment="Center"/>
      <Button Content="Create" Width="120" Click="Create_Click"/>
      <Button Content="Close" Width="80" Click="Close_Click"/>
    </StackPanel>
  </Grid>
</Window>
