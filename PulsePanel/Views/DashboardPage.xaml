
<Page x:Class="PulsePanel.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}">
  <Grid Margin="16">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*"/>
      <ColumnDefinition Width="2*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Dashboard" FontSize="22" FontWeight="Bold" Foreground="White"/>

    <Grid Grid.Row="1" Margin="0,10,10,0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="2*"/>
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Refresh" Command="{Binding Refresh}"/>
      </StackPanel>

      <ListView Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}" >
        <ListView.View>
          <GridView>
            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding InstanceName}" Width="200"/>
            <GridViewColumn Header="Game" DisplayMemberBinding="{Binding GameName}" Width="150"/>
            <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
            <GridViewColumn Header="CPU %" DisplayMemberBinding="{Binding CpuUsage}" Width="70"/>
            <GridViewColumn Header="RAM MB" DisplayMemberBinding="{Binding RamUsage}" Width="80"/>
            <GridViewColumn Header="PID" DisplayMemberBinding="{Binding ProcessId}" Width="60"/>
            <GridViewColumn Header="Actions" Width="220">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal" >
                    <StackPanel.Resources><Style TargetType="Button"><Setter Property="Margin" Value="0,0,6,0"/></Style></StackPanel.Resources>
                    <Button Content="Start" Command="{Binding DataContext.Start, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                    <Button Content="Stop"  Command="{Binding DataContext.Stop,  RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                  </StackPanel>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
          </GridView>
        </ListView.View>
      </ListView>

      <Border Grid.Row="2" Background="{StaticResource ControlBackgroundBrush}" CornerRadius="6" Padding="10">
        <DockPanel>
          <TextBlock Text="Console Output" DockPanel.Dock="Top" FontWeight="Bold"/>
          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ConsoleLines}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </DockPanel>
      </Border>

    </Grid>

    <Border Grid.Column="1" Grid.RowSpan="2" Background="{StaticResource ControlBackgroundBrush}" CornerRadius="6" Padding="12">
      <StackPanel>
        <TextBlock Text="Create Server" FontSize="18" FontWeight="Bold"/>
        <TextBlock Text="Pick blueprint in Marketplace." Foreground="{StaticResource LightTextBrush}"/>
      </StackPanel>
    </Border>
  </Grid>
</Page>
