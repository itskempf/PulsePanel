<Page
    x:Class="Pulse_Panel.Views.CreateServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pulse_Panel.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Pulse_Panel.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewModels:CreateServerViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Create a New Server" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12" />

        <GridView Grid.Row="1" ItemsSource="{Binding Blueprints}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:BlueprintViewModel">
                    <Border BorderBrush="Gray" BorderThickness="1" Width="200" Height="200" Margin="8">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding GameIconPath}" Width="64" Height="64" Margin="0,0,0,12" />
                            <TextBlock Text="{Binding GameName}" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>