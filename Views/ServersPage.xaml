<Page
    x:Class="Pulse_Panel.Views.ServersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pulse_Panel.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Pulse_Panel.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewModels:ServersPageViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: Title -->
        <TextBlock Grid.Row="0" Text="Servers" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12" />

        <!-- Row 1: Action Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox PlaceholderText="Search servers..." MinWidth="200" Margin="0,0,8,0" />
            <ComboBox PlaceholderText="All statuses" MinWidth="150" Margin="0,0,8,0" />
            <Button x:Name="CreateButton" Content="+ Create" Style="{ThemeResource AccentButtonStyle}" Click="CreateButton_Click" />
        </StackPanel>

        <!-- Row 2: Server List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Servers}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Name" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="1" Text="Game" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="2" Text="Status" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="3" Text="Health Score" Style="{StaticResource CaptionTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:ServerViewModel">
                    <Grid Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Image Source="{Binding GameIconPath}" Width="24" Height="24" Margin="0,0,12,0" />
                            <TextBlock Text="{Binding ServerName}" VerticalAlignment="Center" />
                        </StackPanel>
                        
                        <TextBlock Grid.Column="1" Text="{Binding GameName}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center" />
                        <ProgressBar Grid.Column="3" Value="{Binding HealthScore}" Maximum="100" MinWidth="100" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>