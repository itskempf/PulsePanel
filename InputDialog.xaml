<Window x:Class="PulsePanel.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="200" Width="400" WindowStartupLocation="CenterOwner"
        Background="White" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Name="PromptText" Grid.Row="0" Text="Enter value:" FontSize="14" Margin="0,0,0,10"/>
        <TextBox Name="InputTextBox" Grid.Row="1" FontSize="14" Padding="8" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="OK" Width="80" Height="30" Margin="0,0,10,0" 
                    Background="#4A90E2" Foreground="White" BorderThickness="0" Click="Ok_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="80" Height="30" 
                    Background="#95A5A6" Foreground="White" BorderThickness="0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>