<Page
    x:Class="PulsePanel.App.Pages.ServersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:PulsePanel.App.ViewModels"
    xmlns:core="using:PulsePanel.Core.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*" Padding="10">
        <TextBlock Text="Servers" FontSize="20" FontWeight="Bold"/>
    <ListView Grid.Row="1" ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer, Mode=TwoWay}">
            <ListView.ItemTemplate>
        <DataTemplate x:DataType="core:ServerEntry">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="●" Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"/>
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Game}" Foreground="#666"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
